/** PostCSS pipeline for Tailwind CSS v4 (`@tailwindcss/postcss`). Next.js loads this via `next.config` defaults. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
