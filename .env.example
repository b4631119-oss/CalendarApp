# Application title (browser tab + Open Graph). Falls back to README project title if unset.
NEXT_PUBLIC_APP_TITLE=Calendar To-Do — Next.js calendar, events & todos (TypeScript, Tailwind CSS, Framer Motion)

# Canonical site URL for metadata (Open Graph, Twitter, alternates). Default matches README live demo.
NEXT_PUBLIC_SITE_URL=https://taskmate-calendar.vercel.app
